schema = 2
failure = open
allow_legacy_string_bind = true

# Example selective redirect. Legacy kernels require explicit opt-in above.
[org.localsend.localsend_app]
users = 0
processes = *
provider = virtualize
redirect Download/localsend-source -> Download/localsend-redirect
