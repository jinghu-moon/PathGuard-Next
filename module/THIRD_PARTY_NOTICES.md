# Third-party notices

The optional Provider Java hook adapter uses these separately versioned
components:

- LSPlant and DexBuilder, LGPL-3.0, Copyright LSPosed contributors.
- Dobby 1.2, Apache-2.0, Copyright Dobby contributors.
- xDL 2.4.0, MIT, Copyright 2020-2025 HexHacking Team.

License texts are included in the module ZIP under `licenses/`. Corresponding
source and exact provenance are available in `third_party/` in the PathGuard
Next source tree. `libpathguard_lsplant.so` is loaded through a stable C ABI and
can be replaced by an interface-compatible build for debugging or modification.
